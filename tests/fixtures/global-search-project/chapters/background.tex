\section{Background}
\label{sec:background}

The command audit module records a compact event stream for routine terminal activity. It keeps command names, working directories, exit codes, and selected diagnostic messages so that later analysis can reconstruct the operating context.
